<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/add_member_from_contacts"
        android:showAsAction="never"
        android:title="@string/add_member_from_contacts"
        android:checkable="false"/>
    <item
        android:id="@+id/add_member_from_search"
        android:showAsAction="never"
        android:title="@string/add_member_from_search"
        android:checkable="false"/>
</menu>